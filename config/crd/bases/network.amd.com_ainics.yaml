---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: ainics.network.amd.com
spec:
  group: network.amd.com
  names:
    kind: AINIC
    listKind: AINICList
    plural: ainics
    singular: ainic
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.nodesReady
      name: Nodes Ready
      type: integer
    - jsonPath: .status.nodesTotal
      name: Nodes Total
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: AINIC is the Schema for the ainics API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AINICSpec defines the desired state of AINIC
            properties:
              driver:
                description: Driver specifies the AMD AINIC driver configuration
                properties:
                  args:
                    description: Args specifies additional arguments for the driver
                    items:
                      type: string
                    type: array
                  env:
                    description: Env specifies environment variables for the driver
                    items:
                      description: EnvVar represents an environment variable
                      properties:
                        name:
                          description: Name of the environment variable
                          type: string
                        value:
                          description: Value of the environment variable
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  image:
                    description: Image specifies the driver container image
                    type: string
                  version:
                    description: Version specifies the driver version
                    type: string
                required:
                - image
                - version
                type: object
              networkConfig:
                description: NetworkConfig specifies network-related configurations
                properties:
                  mtu:
                    description: MTU specifies the Maximum Transmission Unit
                    format: int32
                    type: integer
                  networkMode:
                    description: NetworkMode specifies the network mode (e.g., "SR-IOV",
                      "DPDK")
                    type: string
                  vfs:
                    description: VFs specifies the number of Virtual Functions to
                      create
                    format: int32
                    type: integer
                  vlan:
                    description: VLAN specifies VLAN configuration
                    items:
                      description: VLANConfig defines VLAN configuration
                      properties:
                        id:
                          description: ID specifies the VLAN ID
                          format: int32
                          type: integer
                        priority:
                          description: Priority specifies the VLAN priority
                          format: int32
                          type: integer
                      required:
                      - id
                      type: object
                    type: array
                type: object
              nodeSelector:
                additionalProperties:
                  type: string
                description: NodeSelector specifies the nodes where AINIC resources
                  should be configured
                type: object
              resources:
                description: Resources specifies resource allocation for AINIC
                properties:
                  cpu:
                    description: CPU specifies CPU allocation
                    type: string
                  hugepagesCount:
                    description: HugepagesCount specifies hugepages count
                    format: int32
                    type: integer
                  hugepagesSize:
                    description: HugepagesSize specifies hugepages size
                    type: string
                  memory:
                    description: Memory specifies memory allocation
                    type: string
                type: object
            required:
            - driver
            type: object
          status:
            description: AINICStatus defines the observed state of AINIC
            properties:
              conditions:
                description: Conditions represents the latest available observations
                  of AINIC state
                items:
                  description: AINICCondition describes the state of AINIC at a certain
                    point
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned
                      format: date-time
                      type: string
                    message:
                      description: Message is a human-readable message indicating
                        details about last transition
                      type: string
                    reason:
                      description: Reason is a unique, one-word, CamelCase reason
                        for the condition's last transition
                      type: string
                    status:
                      description: Status of the condition
                      type: string
                    type:
                      description: Type of AINIC condition
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              message:
                description: Message provides human-readable message about the current
                  state
                type: string
              nodesReady:
                description: NodesReady represents the number of nodes with AINIC
                  ready
                format: int32
                type: integer
              nodesTotal:
                description: NodesTotal represents the total number of nodes targeted
                format: int32
                type: integer
              phase:
                description: Phase represents the current phase of AINIC deployment
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
